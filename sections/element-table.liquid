<div class="elementTable" data-section-id="{{ section.id }}" data-section-type="video-section">
    <h1>{{ section.settings.header }}</h1>

    {% for block in section.blocks %}
        {% assign product_item = all_products[block.settings.product] %}
        {{ product_item.price }}
    {% endfor %}
</div>

{% schema %}
{
    "name": {
        "en": "Element Table"
    },
    "presets": [
        {
            "name": "Element Table",
            "category": "Element Table"
        }
    ],
    "max_blocks": 16,
    "blocks": [
        {
          "type": "element",
          "name": "Element",
          "settings": [
            {
                "type": "select",
                "id": "element-type",
                "label": "Element/Ingredient Type",
                "default": "clear-skin",
                "options": [
                    { "value": "clear-skin", "label": "Clear Skin" },
                    { "value": "other-category", "label": "Other Category" }
                ]
            },
            {
              "type": "text",
              "id": "element-initials",
              "label": "Element/Ingredient Initials"
            },
            {
                "type": "text",
                "id": "element-name",
                "label": "Element/Ingredient Name"
            },
            {
                "type": "textarea",
                "id": "element-description",
                "label": "Element/Ingredient Description"
            }
          ]
        }
    ],
    "settings": [
        {
          "type": "text",
          "id": "header",
          "label": "Header"
        }
    ]
}
{% endschema %}
